const htmlnano = require('./index');
const fs = require('fs');
const preset = require('./lib/presets/max').default;

const options = {};

const html = fs.readFileSync('./github.html', 'utf8');

htmlnano
    .process(html)
    .then(function (result) {
        console.log(result.html);
    })
    .catch(function (err) {
        console.error(err);
    });
